<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.romycomer.bookregal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookRegal">
        <activity android:name=".ComputerSience1"></activity>
        <activity android:name=".Physic" />
        <activity android:name=".BusinessAdministration" />
        <activity android:name=".BusinessInformatic" />
        <activity android:name=".Mathematic" />
        <activity android:name=".ComputerSience" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>